import { AnimeLeave } from './anime-leave';

describe('AnimeLeave', () => {
  it('should create an instance', () => {
    const directive = new AnimeLeave();
    expect(directive).toBeTruthy();
  });
});
