import { AnimeEnter } from './anime-enter';

describe('AnimeEnter', () => {
  it('should create an instance', () => {
    const directive = new AnimeEnter();
    expect(directive).toBeTruthy();
  });
});
