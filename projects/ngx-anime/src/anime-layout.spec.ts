import { AnimeLayout } from './anime-layout';

describe('AnimeLayout', () => {
  it('should create an instance', () => {
    const directive = new AnimeLayout();
    expect(directive).toBeTruthy();
  });
});
